#:kivy 2.0.0

# импорт hex цветов
#:import hex kivy.utils.get_color_from_hex

# Шрифт (цифры) дисплея калькулятора
<DisplayLabel@Label>:
    # размер шрифта
    font_size: '50sp'

    # цвет шрифта
    color: 'black'

    # жирность шрифта
    # bold: True

    # автоматически переносить/растягивать текст {
    text_size: self.size
    halign: 'right'
    valign: 'top'
    # автоматически переносить/растягивать текст }

    # пролистывание текста {
    size_hint_y: None
    height: self.texture_size[1]
    text_size: self.width, None
    padding: 10, 10
    # пролистывание текста }