#:kivy 1.0
# Описываем класс class Solution(TextInput) для файла 013 - CalcKV.py
<Solution>:
    # ----------------------------------------------------------------------
    # параметры конструктора
    # Эти свойства мы указываем в кострукторе при создании объекта
    # если бы не использавли язык графической разметки KV - Kivy Language 
    #
    # в текстовом поле только одна линия для ввода
    multiline: False
    # текстовое поле только для чтения, но не для ввода
    readonly: True
    # горизонтальное выравнивание текста - вправо
    halign: 'right'
    # размер шрифта текста
    font_size: 55
    # начальный текст
    text: 'поле для ввода'
    # ----------------------------------------------------------------------
    # вызываем методы
    #
    # ----------------------------------------------------------------------